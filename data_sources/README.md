# Source data

This repository does not redistribute raw municipal crime datasets. Source data
must be downloaded from the original public portals listed here, then placed in
`data/raw/` if full raw-data reproduction is required.

The included tables, mappings, figures, and metadata are derived benchmark
artifacts for audit and manuscript review.
