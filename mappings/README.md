# Crime category mappings

This folder contains the derived mapping artifacts used by the benchmark pipeline
to harmonize local crime categories with ICCS-compatible categories.

Raw municipal datasets are not redistributed here. The mapping files are derived
research artifacts and should be cited with the repository release.
